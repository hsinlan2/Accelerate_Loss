# Mutation Aware Loss Benchmark (PyTorch + CUDA)

This repository provides a custom CUDA kernel for **per-position, mutation weighted cross-entropy loss** and compares its performance to a standard PyTorch implementation. The benchmark demonstrates significant speedups on GPU.
